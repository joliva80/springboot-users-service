# bootstrap.properties is loaded before application.properties and contains everything related to Config Cloud Sever
spring.application.name=users-service

# profiles: dev pro etc... must exist a file for each one in config server. We can get this from a host enviroment variable rather than hardcooded
spring.profiles.active=dev 
spring.cloud.config.uri=http://localhost:8888
